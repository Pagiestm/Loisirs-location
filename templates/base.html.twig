<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<link rel="icon" type="image/png" href="{{ asset('images/LL-favicon.png') }}" />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Atkinson+Hyperlegible+Mono:ital,wght@0,200..800;1,200..800&display=swap" rel="stylesheet">
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}
        {% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body
        class="m-0! p-0!"
        {{ stimulus_controller('app') }}
        data-app-modal-outlet=".modal-outlet"
    >
		{# Flash messages notifications - Système de pile type Sonner #}
		<div
            {{ stimulus_controller('flashes') }}
            class="fixed top-4 right-4 z-9999 pointer-events-none"
            style="width: 400px; height: 0;"
	    >
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <twig:Flash
                        type="{{ label }}"
                        message="{{ message }}"
                    />
                {% endfor %}
            {% endfor %}
        </div>

		<twig:Navbar />

		{% block body %}{% endblock %}

		<twig:Footer />

		{# Modale de connexion/inscription #}
		{% if not is_granted('IS_AUTHENTICATED_FULLY') %}
			<twig:Modal id="auth" size="sm" initiallyOpen="{{ app.request.query.get('open_login') == 'true' }}">
				<twig:Security:Index
                    loading="lazy"
                    key="auth-component"
                />
			</twig:Modal>
		{% endif %}
	</body>
</html>
