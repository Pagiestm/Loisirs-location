<footer class="bg-brown text-beige font-body">
    <div class="max-w-7xl mx-auto px-6 py-12 grid grid-cols-1 md:grid-cols-3 gap-10">

        {# Bloc identité #}
        <div class="group w-full flex items-center justify-center">
            <div class="w-40 h-40">
                <img src="{{ asset('images/LL-logo.png') }}" alt="Loisirs Location" class="w-full h-full object-contain" />
            </div>
        </div>

        {# Liens utiles #}
        <div>
            <h3 class="font-display text-lg text-white mb-4">Navigation</h3>
            <ul class="space-y-2 text-sm">
                <li><a href="{{ path('app_home') }}" class="hover:text-primary transition-colors">Accueil</a></li>
                <li><a href="{{ path('app_vans_index') }}" class="hover:text-primary transition-colors">Nos vans</a></li>
                <li><a href="{{ path('app_blog_index') }}" class="hover:text-primary transition-colors">Blog</a></li>
                <li><a href="#devis" class="hover:text-primary transition-colors">Demander un devis</a></li>
                <li><a href="{{ path('app_contact') }}" class="hover:text-primary transition-colors">Contact</a></li>
                <li><a href="#" class="hover:text-primary transition-colors">Mentions légales</a></li>
                <li><a href="#" class="hover:text-primary transition-colors">Politique de confidentialité</a></li>
            </ul>
        </div>

        {# Contact & Réseaux #}
        <div>
            <h3 class="font-display text-lg text-white mb-4">Nous contacter</h3>
            <ul class="space-y-2 text-sm">
                <li>
                    <a href="mailto:contact@loisirs-location.fr" class="hover:text-primary transition-colors flex items-center gap-2">
                        <twig:ux:icon name="lucide:mail" class="w-4 h-4 shrink-0" aria-hidden="true" />
                        contact@loisirs-location.fr
                    </a>
                </li>
                <li class="flex items-center gap-2">
                    <twig:ux:icon name="lucide:map-pin" class="w-4 h-4 shrink-0" aria-hidden="true" />
                    Hauts-de-France
                </li>
            </ul>

            {# Réseaux sociaux #}
            <div class="flex gap-4 mt-5">
                <a href="#" aria-label="Facebook Loisirs Location" class="text-beige hover:text-primary transition-colors">
                    <twig:ux:icon name="simple-icons:facebook" class="w-5 h-5" aria-hidden="true" />
                </a>
            </div>
        </div>
    </div>

    {# Barre basse #}
    <div class="border-t border-white/10 py-4 text-center text-xs text-beige/60">
        © {{ "now"|date("Y") }} Loisirs Location — Tous droits réservés
    </div>
</footer>
