# Connexion à la base de données PostgreSQL via PgAdmin (Docker)

- Nom du serveur : loisirs-location (ou ce que vous voulez)
- Hôte / Adresse : database
- Port : 5432
- Utilisateur : loisirs
- Mot de passe : loisirs123
- Base de données : loisirs-location

Ces informations correspondent à la configuration par défaut du projet (voir .env et compose.yaml).

Astuce : l’hôte est le nom du service Docker (database), pas localhost.
